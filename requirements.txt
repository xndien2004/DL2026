# --- Core DL ---
# IMPORTANT: For CUDA build, install torch + torchvision FIRST from
# https://pytorch.org/get-started/locally/  (the plain `torch` wheel below is CPU-only).
torch>=2.0
torchvision>=0.15

# --- Detection ---
ultralytics>=8.3
ensemble-boxes>=1.0.9

# --- Data / augmentation ---
albumentations>=1.3
opencv-python>=4.8
pyyaml>=6.0
Pillow>=10.0

# --- Numerics / dataframes ---
numpy>=1.24,<2.3
pandas>=2.0

# --- Plotting ---
matplotlib>=3.7
seaborn>=0.13

# --- Misc ---
tqdm>=4.66

# --- Dataset download ---
huggingface_hub>=0.20
